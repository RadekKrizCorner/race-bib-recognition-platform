# Samples

Only public, synthetic, generated, or owner-approved images may be placed in `samples/photos`.

The current repository uses deterministic byte-based demo input so no private race photos are required.
