[project]
name = "rbp-contracts"
version = "0.1.0"
description = "Shared contracts for Race Bib Recognition Platform services."
requires-python = ">=3.14"
dependencies = ["pydantic>=2.12.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
